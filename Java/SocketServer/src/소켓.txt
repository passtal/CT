소켓이란 네트워크 통신을 위한 엔드포인트
=
소켓 → 네트워크를 통해 데이터를 주고받기 위한 끝점.

클라이언트에서 연결 요청

서버에서 연결

다음 클라이언트로부터 연결 요청을 기다림

x반복 . . . 

IP 주소와 포트 번호의 조합으로 식별

네트워크 프레이밍이 가능하도록 추상화된 API를 제공

In JAVA → Socket클래스 (클라이언트) 와 ServerSocket 클래스 (서버) 를 통해 소켓 프로그래밍을 구현 가능

소켓 → 전송 계층과 응용 계층 사이의 연결망 역할 (전송 세션 표현 응용)

프로토콜 → 통신 규악 (TCP UDP) // TCP - 연결지향 , UDP - 비연결지향

소켓통신도 TCP임

서버 - 클라이언트 모델

서버 → 클라이언트의 연결 요청을 대기하고 수락

클라이언트 → 서버에 연결을 요청하고 데이터 송.수신

인바운드 / 아웃바운드 → 방화벽에 규칙 적용

